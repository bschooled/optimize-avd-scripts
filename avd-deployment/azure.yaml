# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: avd-pooled-deployment
metadata:
  template: avd-pooled-deployment@0.0.1

infra:
  provider: bicep
  path: infra
  module: main

services: {}
